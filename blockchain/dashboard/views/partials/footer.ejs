    </div>
    
    <footer class="footer mt-auto py-3 bg-light">
        <div class="container text-center">
            <span class="text-muted">Keralis Dashboard &copy; <%= new Date().getFullYear() %></span>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/socket.io/socket.io.js"></script>
    <script src="/js/dashboard.js"></script>
    <script>
        console.log('Footer chargé, vérification des scripts:');
        console.log('- Bootstrap:', typeof bootstrap !== 'undefined' ? 'OK' : 'NON CHARGÉ');
        console.log('- Socket.IO:', typeof io !== 'undefined' ? 'OK' : 'NON CHARGÉ');
        
        // Vérifier si dashboard.js est chargé
        if (typeof updateStats === 'undefined') {
            console.error('ERREUR: dashboard.js n\'est pas chargé correctement');
            document.body.innerHTML += '<div class="alert alert-danger">Erreur: dashboard.js n\'est pas chargé</div>';
        } else {
            console.log('- dashboard.js: OK');
        }
    </script>
</body>
</html> 